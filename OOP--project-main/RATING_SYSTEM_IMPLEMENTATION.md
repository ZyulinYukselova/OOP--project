# Рейтингова система - Имплементация

## Дата: 2024
## Статус: ✅ ЗАВЪРШЕНА

---

## Какво беше имплементирано:

### 1. ✅ RatingService.java (Нов файл)

Създаден е нов сервис `RatingService` с методи за оценяване:

#### Методи:
- **`rateCompany(User raterActor, String companyId, double rating)`**
  - Оценяване на компании
  - Кой може: Дистрибутори (които имат одобрени пътувания) или Администратори
  - Валидация: Проверява дали дистрибуторът има одобрени пътувания с компанията

- **`rateDistributor(User raterActor, String distributorId, double rating)`**
  - Оценяване на дистрибутори
  - Кой може: Компании (които са одобрили заявки) или Администратори
  - Валидация: Проверява дали компанията е одобрила заявки от дистрибутора

- **`rateCashier(User raterActor, String cashierId, double rating)`**
  - Оценяване на касиери
  - Кой може: Дистрибутори (собственици на касиера) или Администратори
  - Валидация: Проверява дали дистрибуторът е собственик на касиера

#### Валидация:
- Рейтингова скала: **1.0 - 5.0**
- Валидация на права: Кой може да оценява кого
- Валидация на стойност: Рейтингът трябва да е между 1.0 и 5.0
- Бизнес правила: Проверка за работна история (одобрени пътувания/заявки)

---

### 2. ✅ CLI команди (Cli.java)

#### Нови команди:
- **`rate-company <companyId> <rating>`**
  - Оценява компания
  - Пример: `rate-company <id> 4.5`

- **`rate-distributor <distributorId> <rating>`**
  - Оценява дистрибутор
  - Пример: `rate-distributor <id> 5.0`

- **`rate-cashier <cashierId> <rating>`**
  - Оценява касиер
  - Пример: `rate-cashier <id> 3.5`

#### Подобрения:
- Добавена информация в `help` командата за рейтинговата скала
- Валидация на входни данни (проверка за валиден рейтинг)
- Показване на ясни съобщения за грешки

---

## Бизнес правила:

### Кой може да оценява кого:

1. **Дистрибутори → Компании**
   - Само ако имат одобрени пътувания заедно
   - Работили са поне веднъж

2. **Компании → Дистрибутори**
   - Само ако са одобрили заявки от дистрибутора
   - Има работна история

3. **Дистрибутори → Касиери**
   - Само собственици на касиера
   - Пряк контрол над касиера

4. **Администратори → Всички**
   - Могат да оценяват всеки
   - Без ограничения

---

## Валидация:

### Рейтингова скала:
- **Минимум**: 1.0
- **Максимум**: 5.0
- **Тип**: `double`

### Проверки:
- ✅ Рейтингът трябва да е число
- ✅ Рейтингът трябва да е между 1.0 и 5.0
- ✅ Потребителят трябва да има права за оценяване
- ✅ Проверка за работна история (където е приложимо)

---

## Примери за употреба:

### Оценяване на компания (от дистрибутор):
```
> rate-company <companyId> 4.5
Company rated: id=<id> name=CityTransport new rating=4.5
```

### Оценяване на дистрибутор (от компания):
```
> rate-distributor <distributorId> 5.0
Distributor rated: id=<id> name=BestTickets new rating=5.0
```

### Оценяване на касиер (от дистрибутор):
```
> rate-cashier <cashierId> 3.5
Cashier rated: id=<id> name=FrontDesk new rating=3.5
```

### Грешен рейтинг:
```
> rate-company <id> 6.0
Error: Rating must be between 1.0 and 5.0
```

### Липса на права:
```
> rate-company <id> 4.0
Error: Can only rate companies you have worked with (approved trips)
```

---

## Технически детайли:

### Файлове:
- **Нов файл**: `RatingService.java`
- **Модифициран**: `Cli.java`

### Импортове:
- `RatingService` се импортира чрез `import com.transport.ticketing.service.*;`

### Константи:
- `MIN_RATING = 1.0`
- `MAX_RATING = 5.0`
- Публични методи `getMinRating()` и `getMaxRating()`

---

## Статус:

✅ **Рейтинговата система е напълно имплементирана и готова за използване!**

- Всички методи са реализирани
- CLI командите работят
- Валидацията е на място
- Бизнес правилата са имплементирани
- Няма грешки от линтера

---

## Следващи стъпки (по желание):

1. **Усредняване на рейтинги**: Може да се добави логика за изчисляване на среден рейтинг от множество оценки
2. **История на оценки**: Може да се добавят записи за кой кога е оценил кого
3. **Ограничение на повторно оценяване**: Може да се добави правило за един рейтинг на потребител

---

**Рейтинговата система е готова и работи!** 🎉
